# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(clash STATIC 
  clash.c
  response.c)

include(Tornado.cmake)
set_tornado(clash)

target_include_directories(clash PUBLIC ../include)


target_link_libraries(clash PUBLIC 
  tinge)

